name: Build Example C0re

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential binutils

      - name: Build
        run: make

      - name: Verify outputs
        run: |
          ls -lah
          file example_C0re.elf example_C0re.bin
          wc -c example_C0re.bin

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: example_C0re-build
          path: |
            example_C0re.elf
            example_C0re.bin
          if-no-files-found: error
